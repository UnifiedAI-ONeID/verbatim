schema:
  source: schema.gql
location: us-central1

connector:
  - id: firebase
    type: firebase

authorization:
  # Who can perform what actions on the schema.
  # This is a set of rules evaluated in order.
  # The first rule that matches a request determines the result.
  # If no rules match, the request is denied.
  rules:
    # - claim: # Check for a claim in the Firebase Auth token.
    #     # For example, to check for a custom claim:
    #     # has: permissions.posts.read
    #     has: email # Grant access if the user has an email claim.
    - role: EndUser
      actions: [read, write, delete, execute]
      # operations: # The operations that this rule applies to.
      #   - users.get # Format: {SchemaType.operation}


# generated-by: firebase-cli
